git:
  {{- if .is_work }}
  autoFetch: false
  fetchAll: false
  {{- end }}
  diffRenderers:
    - colorArg: always
      command: delta --paging=never --dark
{{- if .is_work }}
disableStartupPopups: true
{{- end }}
